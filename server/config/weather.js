// Central place for weather defaults — never scatter coordinates/units
// across providers, services, or routes.
export const DEFAULT_WEATHER_LOCATION = {
  name: 'London',
  country: 'GB',
  latitude: 51.5074,
  longitude: -0.1278,
};

export const WEATHER_UNITS = 'metric';
